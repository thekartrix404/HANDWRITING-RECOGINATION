# HANDWRITING-RECOGINATION
Handwriting Recognition System is a Python-based machine learning project that uses the MNIST dataset to recognize handwritten digits. The system uses CNN algorithms with TensorFlow, Keras, and OpenCV to process images, train the model, and accurately predict digits from 0–9 in real time.
